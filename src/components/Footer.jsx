import "../assets/styles/Footer.css"

const Footer=()=> {
    return (
        <footer className="footer-container">
            <a > "La lengua sirve para más que comunicarnos; nos permite conocer la historia y ser parte de ella."</a>
        </footer>
    );
}

export default Footer